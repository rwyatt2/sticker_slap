# Database
# Primary connection (use connection pooler like PgBouncer in production)
DATABASE_URL="postgresql://user:password@localhost:5432/sticker_slap?schema=public"

# Direct connection for migrations (bypasses connection pooler)
# Required when using Prisma Migrate with a connection pooler
DIRECT_DATABASE_URL="postgresql://user:password@localhost:5432/sticker_slap?schema=public"

# NextAuth.js
# Generate with: openssl rand -base64 32
AUTH_SECRET="your-auth-secret-generate-with-openssl-rand-base64-32"
AUTH_URL="http://localhost:3000"
NEXTAUTH_URL="http://localhost:3000"

# OAuth - Google
# Create credentials at: https://console.cloud.google.com/apis/credentials
AUTH_GOOGLE_ID="your-google-client-id"
AUTH_GOOGLE_SECRET="your-google-client-secret"

# OAuth - GitHub
# Create OAuth App at: https://github.com/settings/developers
AUTH_GITHUB_ID="your-github-client-id"
AUTH_GITHUB_SECRET="your-github-client-secret"

# Email Service (optional - emails are logged to console in development)
# For production, use a service like Resend, SendGrid, or AWS SES
# EMAIL_SERVICE_CONFIGURED="true"
# RESEND_API_KEY="re_xxxxx"
# EMAIL_FROM="noreply@yourdomain.com"

# AWS S3 (or use R2 section below)
AWS_ACCESS_KEY_ID="your-aws-access-key"
AWS_SECRET_ACCESS_KEY="your-aws-secret-key"
AWS_REGION="us-east-1"
AWS_S3_BUCKET="your-bucket-name"

# Cloudflare R2 (alternative to S3)
# R2_ACCESS_KEY_ID="your-r2-access-key"
# R2_SECRET_ACCESS_KEY="your-r2-secret-key"
# R2_ENDPOINT="https://your-account-id.r2.cloudflarestorage.com"
# R2_BUCKET="your-r2-bucket"

# Upstash Redis (for rate limiting)
UPSTASH_REDIS_REST_URL="your-upstash-redis-url"
UPSTASH_REDIS_REST_TOKEN="your-upstash-redis-token"

# Redis URL (for BullMQ job queue - optional, falls back to sync processing)
# REDIS_URL="redis://localhost:6379"

# Virus Scanning (optional)
# Options: 'clamav' | 'virustotal' | 'mock' | 'none'
# Defaults to 'mock' in development, 'none' in production
VIRUS_SCANNER="mock"

# ClamAV Configuration (if VIRUS_SCANNER=clamav)
# CLAMAV_HOST="localhost"
# CLAMAV_PORT="3310"

# VirusTotal Configuration (if VIRUS_SCANNER=virustotal)
# Get API key at: https://www.virustotal.com/gui/my-apikey
# VIRUSTOTAL_API_KEY="your-virustotal-api-key"

# App
NEXT_PUBLIC_APP_URL="http://localhost:3000"
NODE_ENV="development"
